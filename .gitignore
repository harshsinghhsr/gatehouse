node_modules/
dist/
.next/
.env
.env.*
!.env.example
*.log

# Prisma 7 generates the client into the source tree
apps/api/src/generated/
*.tsbuildinfo

# The examples leave one usable gateway key on disk; it must never be committed
examples/.gatehouse-key
examples/.gatehouse-session
