{
  "name": "gatehouse-examples",
  "private": true,
  "type": "module",
  "description": "Runnable examples for Gatehouse. Deliberately outside the repo's workspaces, so a contributor's npm install never pulls the vendor SDKs.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "openai": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "zod": "^4.4.3"
  }
}
